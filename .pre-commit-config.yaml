repos:
  - repo: local
    hooks:
      - id: llm-managed-project-contract
        name: LLM managed project contract
        entry: ../../scripts/init-project.sh --check AutoDrill
        language: system
        pass_filenames: false
